<nav class="navbar navbar-expand-lg bg-body-tertiary my_navbar">
  <div class="container-fluid">
    <%= link_to "OurClinic", root_path, class: "navbar-brand my-navbar-brand" %>
    <button class="navbar-toggler navbar-toggler-my custom-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "nav-link my-nav-item" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Log in", new_user_session_path, class: "nav-link  my-nav-item" %>
          </li>
        <% end %>
        <li class="nav-item">
          <%= link_to "Appointments", appointments_path(@appointments), class: "nav-link  my-nav-item" %>
        </li>
      </ul>
    </div>
  </div>
</nav>